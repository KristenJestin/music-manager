node_modules
dist
.output
.nitro
.tanstack
.local
bun.lock
apps/web/src/routeTree.gen.ts
apps/web/drizzle
packages/contracts/toolbox
services/toolbox/.venv
services/toolbox/uv.lock

# recorded source responses and golden projections — byte-identical to what
# `bun run --cwd packages/domain fixtures:record` and MM_UPDATE_GOLDEN=1 write
packages/domain/fixtures
packages/domain/golden

# recorded Navidrome answers — byte-identical to what
# `bun run navidrome:record` writes, so a re-recording is a readable diff
apps/web/src/server/integrations/navidrome/cassettes
