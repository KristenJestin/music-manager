# dependencies
node_modules/

# generated by the TanStack Router plugin on dev/build/check — never edited by hand
apps/web/src/routeTree.gen.ts

# build output
dist/
build/
.output/
.nitro/
.tanstack/
.vinxi/
.vite/
*.tsbuildinfo

# local runtime state (postgres data, navidrome data, music library bind mount)
.local/

# environment (secrets never enter this repo — see .env.example)
.env
.env.*
!.env.example
!.env.production.example

# python / uv (the toolbox runs in Docker; a local venv is only for lint & tests)
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.pyright/

# test & tooling artifacts
coverage/
test-results/
playwright-report/
.eslintcache

# editors / OS
.DS_Store
Thumbs.db
*.swp
