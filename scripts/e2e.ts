/**
 * `bun run e2e` — placeholder.
 *
 * The offline fixture end-to-end run (`mm import` on the Discovery fixture) arrives in P03,
 * and the Playwright browser scenarios in P06. Until then this exits 0 so that the command
 * exists in every phase's Definition of Done without lying about what it proves.
 */
console.log("no e2e yet (arrives in P03 for the CLI, P06 for the browser)");
process.exit(0);
